# Work

Complete exactly one checklist item during this invocation.

1. Read `CHECKLIST.md` and `PROGRESS.md` to understand the current state and prior handoffs.
2. Choose the highest-priority unchecked item, considering user impact, dependencies, and risk rather than simply selecting the first item.
3. Mark only that item in progress by changing its marker from `[ ]` to `[/]`.
4. Implement and verify only that item. Do not begin another checklist item.
5. Mark the item complete by changing its marker from `[/]` to `[x]` only after its verification passes.
6. Append a concise handoff note to `PROGRESS.md` describing the completed item, verification performed, and any useful follow-up context.
7. Commit the completed implementation, checklist update, and progress note together in one git commit.
